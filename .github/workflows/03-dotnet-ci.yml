name: dotnet-ci

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Setup .NET
      uses: actions/setup-dotnet@v5

    - name: Build
      run: dotnet build ./GitHubActions-PracticeRepo/GitHubActions-PracticeRepo.csproj

    - name: Test
      run: dotnet test ./GitHubActions-PracticeRepo.Tests/GitHubActions-PracticeRepo.Tests.csproj --logger "console;verbosity=detailed"